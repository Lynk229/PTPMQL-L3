@model IEnumerable<DemoMvc.Models.Thisinh>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<p>
    <a asp-action="Create">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ThisinhID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fullname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ThisinhID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fullname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ThisinhID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ThisinhID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ThisinhID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>